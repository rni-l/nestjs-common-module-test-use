import { TestInterceptorInterceptor } from './test-interceptor.interceptor';

describe('TestInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new TestInterceptorInterceptor()).toBeDefined();
  });
});
